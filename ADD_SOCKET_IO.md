# Install Socket.IO Client untuk Typing Indicators

Untuk menggunakan typing indicators, perlu install socket.io-client:

```bash
cd admin
npm install socket.io-client
```

Setelah install, typing indicators akan bekerja otomatis di chat page.

